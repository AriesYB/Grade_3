server:
  port: 80
spring:
  application:
    name: client-ribbon
logging:
  level:
    root: info
eureka:
  instance:
    instance-id: client-ribbon
  client:
    service-url:
      defaultZone: http://eureka1.com:7001/eureka,http://eureka2.com:7002/eureka